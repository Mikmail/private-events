%strong
  = @event.title 
%br 
  = @event.description
%br 
  = @event.location
%br 
  = @event.date
%br
  = @event.attandee
%br 
  - if current_user && @event.creator != current_user && !@event.attendees.include?(current_user)
    = link_to "Attend event", create_attending_event_path(:user_id => current_user.id, :id => @event.id), method: :post, class: "button is-info"
  - else 
    %strong You have already registered for this event
%br

= link_to events_path do 
  Back

