%h1 Your events
%p  
  - if current_user 
    - if @events.any?
      - @events.each do |event| 
        = link_to event.title, event
    - else
      %h1 You have no pending events
      = link_to "Create event", new_event_path  